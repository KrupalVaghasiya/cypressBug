name: GitHub Actions Demo
on: 
  push:
    branches:
      - 'master'

jobs:
 cypress-test:
  name: run on windows
  runs-on: windows-latest
  steps: 
    - uses: actions/checkout@v2

    - name: Install dependencies
      run: |
        npm install
        npm install --dev
        npm run record-test
